services:
  oracle-log-exporter:
    image: ghcr.io/tulonbaar/dock-app.oracle-log-exporter:${GHCR_TAG-latest}
    container_name: oracle-log-exporter
    env_file:
      - .env
    volumes:
      - ./output:/app/output
      - ./state:/app/state
    logging:
      driver: json-file
      options:
        max-size: ${DOCKER_LOG_MAX_SIZE:-10m}
        max-file: ${DOCKER_LOG_MAX_FILE:-5}
    restart: unless-stopped
